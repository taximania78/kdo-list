import type { Metadata } from 'next';
import { Nav } from '@/components/Nav';
import { Geist, Geist_Mono, Mountains_of_Christmas } from 'next/font/google';
import './globals.css';

const theme = process.env.THEME || 'default';

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
});

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
});

const mountains_of_christmas = Mountains_of_Christmas({
  weight: '700',
  subsets: ['latin'],
});

export const metadata: Metadata = {
  title:
    theme === 'christmas' ? 'Joyeux Liste de Noël !' : "Liste d'anniversaire",
  description: 'Generated by create next app',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="fr-FR">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <Nav />
        {children}
        <footer>
          <div className="container mx-auto items-center justify-center">
            <div className={mountains_of_christmas.className}>
              <p className="text-center text-3xl font-bold text-red-500 underline decoration-wavy decoration-2 decoration-green-600">{`${theme == 'christmas' ? 'Joyeux Noël !' : ''}`}</p>
              <br />
            </div>
          </div>
        </footer>
      </body>
    </html>
  );
}
